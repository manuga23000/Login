const User: React.FC = () => {
    return (
        <div>
            <h1>Welcome, User!</h1>
            {/* Agrega aquí el contenido de la página para usuarios */}
        </div>
    );
};

export default User;
